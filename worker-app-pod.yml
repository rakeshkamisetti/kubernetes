apiversion: v1
kind: Pod
metadata:
  name: wroker-app-pod
  labels:
    name: wroker-app-pod
    app: demo-voting-app

spec:
  containers:
    - name: worker-app
      image: dockersamples/examplevotingapp_worker
